pipeline {
  agent any
  stages ('Scan and Build Jar File') {
    stage {
      steps {
        withSonarQubeEnv(installationName: 'sonrq1', credentialsId: 'SonarQubeToken') {
        sh 'mvn clean package sonar:sonar'
        }
      }
    } 
  }
}
